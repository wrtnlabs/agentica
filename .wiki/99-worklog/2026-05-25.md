# 2026-05-25 작업 로그

## 요약

- Claude Code 최적화 지도에서 다음 우선순위로 잡은 Context Projector/Result Budget의 1차 구현을 시작했다.
- public history를 변경하지 않고 model-facing context projection에만 opt-in result budget을 적용했다.
- `MicroAgentica` direct path는 유지했다.

## 구현

- `IAgenticaConfig.context.resultBudget`
  - `maxResultCharacters`
  - `preserveRecentResults`

- `decodeHistory`/`decodeHistories`
  - 단일 history decode는 기본 동작을 유지
  - opt-in result budget이 있으면 큰 execute result를 preview/reference object로 projection
  - 여러 history projection에서는 최근 execute result 보존 정책 적용

- Agentica orchestration 연결
  - initialize
  - select
  - cancel
  - call
  - describe

## 검증

```bash
pnpm --filter @agentica/core exec vitest run src/factory/histories.spec.ts
pnpm --filter @agentica/core exec tsc --noEmit
pnpm --filter @agentica/core test -- --run
pnpm --filter @agentica/core lint
pnpm --filter @agentica/core build
```

결과:

- focused histories test: 4 passed
- core tsc: pass
- core test: 153 passed
- core lint: exit 0, 기존 `functional/*` JSDoc warning 20개 유지
- core build: exit 0, 기존 rollup external dependency warning 유지

## 후속 작업

- `AgenticaResultStore`를 실제 runtime state로 분리한다.
- result reference에 digest/version을 붙이고 unchanged stub을 지원한다.
- segmented content reader를 추가한다.
- chat renderer의 large result display를 preview/reference로 바꾼다.
- compact restore attachment에 result refs를 연결한다.
