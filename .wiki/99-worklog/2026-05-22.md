# 2026-05-22 작업 로그

## 위키 운영 규칙 수립

- 사용자 지시에 따라 `.wiki` 폴더를 생성하고 다단 구조의 마크다운 위키를 운영하기 시작했다.
- 초기 문서는 색인, 프로젝트 개요, 워크스페이스 구조, 작업 로그로 구성했다.
- 이후 작업에서는 코드 이해 과정에서 확인한 장기 지식은 주제 문서에, 해당 날짜의 진행 기록은 작업 로그에 반영한다.

## Claude Code 분석과 Agentica 적용 방향

- `/home/samchon/github/samchon/claude-code` snapshot의 query loop, tool abstraction, context compaction, ToolSearch, file-based memory/skill 구조를 조사했다.
- Agentica의 현재 `Agentica`, `MicroAgentica`, `orchestrate/*`, `@agentica/vector-selector` 구조와 비교했다.
- 장기 방향은 기존 선형 executor를 stateful loop로 바꾸고, core에 DBMS 없는 local operation index를 먼저 넣는 것으로 정리했다.
- 분석 문서:
  - `02-research/claude-code/architecture.md`
  - `03-design/agentica-next/roadmap.md`
  - `03-design/agentica-next/local-rag-selector.md`
  - `03-design/agentica-next/context-compaction.md`

## MicroAgentica 범위 제외 원칙

- 사용자 지시에 따라 `MicroAgentica`는 현 상태를 유지하는 것으로 확정했다.
- 향후 Claude Code식 runtime 강화, local selector, context compaction은 `Agentica`에 한정한다.
- 공통 유틸리티를 추가하더라도 `MicroAgentica`의 facade, orchestration, 사용 경험은 변경하지 않는다.

## 위키 전용 정밀 설계 개정

- 사용자가 "지금은 wiki 개정만"이라고 범위를 정정하여 코드 구현은 진행하지 않았다.
- Claude Code 분석을 query loop, tool system, context/RAG/memory로 분리해 세부 문서를 추가했다.
- Agentica 적용 설계를 아키텍처 청사진과 orchestration 재설계 문서로 구체화했다.
- 주요 결론:
  - `Agentica`는 stateful runtime loop로 가야 한다.
  - `call.ts`의 validation feedback loop는 유지한다.
  - local operation index는 DBMS 없는 core 내부 기반으로 설계한다.
  - context 압축은 summary만이 아니라 operational restore까지 포함해야 한다.
  - `MicroAgentica`는 변경 대상에서 제외한다.

## 장기 목표용 지식저장소 보강

- 목표 지속 컨텍스트에 따라 `.wiki`를 완성형 인수인계 지식저장소로 확장하기 시작했다.
- 위키 운영 매뉴얼, 전수정독 진행판, 완료 조건과 검증 기준을 추가했다.
- Agentica 소스 인벤토리와 패키지 인벤토리를 추가했다.
- core 구조 지도와 영향면 분석을 추가했다.
- Claude Code source inventory를 추가해 아직 읽어야 할 큰 영역을 명시했다.

## Core 계약과 Claude Code AgentTool 반영

- Agentica core functional/controller 계약을 별도 문서로 정리했다.
- Agentica event/history/JSON serialization 계약과 새 event 추가 체크리스트를 정리했다.
- Agentica 현재 orchestration과 prompt 구조를 initialize/select/cancel/call/describe 단계별로 정리했다.
- `@agentica/vector-selector` 현재 구조와 DBMS/API 의존 한계를 정리했다.
- Claude Code `AgentTool` lifecycle, agent definition, async/background, fork, memory 구조를 분석했다.
- Claude Code `BashTool` policy 구조를 Agentica operation policy 설계 근거로 정리했다.
- 위 분석을 Agentica Next sub-agent/runtime 설계 문서로 번역했다.
- 사용자 지시에 따라 이번 라운드는 `.wiki` 개정만 수행했고 source package 구현은 하지 않았다.

## Request/Streaming과 File Tool 정책 보강

- Agentica core request wrapper, streaming merge, token usage aggregation, AsyncQueue/MPSC 구조를 정리했다.
- Claude Code `FileReadTool`, `GrepTool`, `FileEditTool`의 permission, read state, result projection, freshness check 구조를 분석했다.
- 해당 분석을 Agentica operation policy/state 설계로 번역했다.
- 여전히 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Compact와 Public Surface 세부 개정

- Claude Code `src/services/compact/*` 전체를 다시 읽어 compact를 별도 정밀 분석 문서로 분리했다.
- full compact, partial compact, session memory compact, auto compact, microcompact, API-native clearing, post-compact cleanup을 하나의 lifecycle로 정리했다.
- compact boundary, preserved segment relink, QueryEngine pruning, prompt-too-long API-round retry, post-compact restore attachments를 Agentica 설계 근거로 기록했다.
- Agentica `packages/rpc`와 `packages/chat`의 public surface를 읽고, compact/event/history 확장 시 영향을 별도 문서로 정리했다.
- 결론은 1차 compact를 Agentica-only internal runtime state와 짧은 `systemMessage` marker로 시작하고, structured compact history/event/RPC 확장은 별도 phase로 나누는 것이다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Benchmark/Test 검증 전략 보강

- `packages/benchmark`의 select/call/micro benchmark, predicator, reporter, structures를 정독해 현재 품질 측정 모델을 문서화했다.
- `benchmark/vector-selector-benchmark`의 plain/pg/sqlite selector comparison과 shopping scenario를 정리했다.
- `test` runner, env handling, base/streaming/validate/RPC/benchmark 주요 feature tests를 읽고 현재 e2e 검증 범위를 정리했다.
- Agentica Next phase별 deterministic unit, e2e, benchmark gate를 별도 검증 전략 문서로 만들었다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Claude Code Write/Task 도구 보강

- Claude Code `FileWriteTool`, `GlobTool`, `NotebookEditTool`, `TodoWriteTool`, `TaskOutputTool`을 정독해 write/search/artifact/task output 정책을 정리했다.
- Todo V2 관련 `TaskCreateTool`, `TaskUpdateTool`, `TaskGetTool`, `TaskListTool`, `utils/tasks.ts`, task output storage/framework도 함께 읽었다.
- write 계열의 call-time stale check, notebook structured mutation, task JSON lock/high-water mark, output reference/tail/truncation 원칙을 Agentica operation policy/state 설계에 반영했다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Claude Code LSP/Task Control 보강

- Claude Code `LSPTool`, LSP manager/server manager, diagnostics registry를 읽고 optional semantic retrieval과 async diagnostics channel을 문서화했다.
- `TaskStopTool`과 `tasks/stopTask.ts`를 읽고 background task cancellation semantics를 정리했다.
- 독립 `LS` tool은 확인되지 않았고, `ls` command는 BashTool path validation 영역으로 다루는 것이 맞다고 진행판에 정정했다.
- Agentica local selector에는 LSP를 optional semantic layer로만 두고, task stop은 runtime control transition으로 보는 방향을 설계 문서에 반영했다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Claude Code Plan/Config/Skill/Web/MCP/Team 도구 보강

- Claude Code `AskUserQuestion`, `EnterPlanMode`, `ExitPlanModeV2`, `Config`, `EnterWorktree`, `ExitWorktree`, `Skill`, `SendUserMessage`, `StructuredOutput`, `Sleep` 계열을 읽고 session control 도구 문서로 정리했다.
- `WebFetch`, `WebSearch`, `MCPTool`, `ListMcpResources`, `ReadMcpResource`, `McpAuth`, `RemoteTrigger`, `CronCreate/Delete/List`를 읽고 external connector 도구 문서로 정리했다.
- `TeamCreate`, `TeamDelete`, `SendMessage`를 읽고 team/task/mailbox/message routing 도구 문서로 정리했다.
- Agentica Next 설계 문서에 runtime mode, user interaction, config registry, connector metadata, team/mailbox runtime을 추가했다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Claude Code MCP Service와 Skill/Plugin Loading 보강

- Claude Code `services/mcp/client.ts`, `config.ts`, `types.ts`, `useManageMCPConnections.ts`, channel permission/notification 구조를 읽고 MCP를 connector runtime으로 정리했다.
- transport lifecycle, config cascade, auth/session state, tool/prompt/resource discovery, large result projection, hot-swap registry를 `02-research/claude-code/mcp-service-runtime.md`에 문서화했다.
- Claude Code `skills/loadSkillsDir.ts`, bundled/builtin skill registry, plugin command/skill loader, plugin manifest schema, plugin MCP integration, SkillTool 실행 경로를 읽었다.
- frontmatter-first discovery, lazy skill body loading, plugin capability pack, MCP skill/command merge, forked skill execution을 `02-research/claude-code/skill-plugin-loading.md`에 문서화했다.
- Agentica Next 설계에는 connector registry lifecycle, capability pack manifest, capability permission, lazy capability loading, registry public-surface 보류 전략을 추가했다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Claude Code Task/Coordinator Runtime 보강

- Claude Code `Task.ts`, `tasks.ts`, `utils/task/framework.ts`, `utils/task/diskOutput.ts`를 읽고 task contract, disk-backed output, output offset, notification queue, terminal eviction 원칙을 정리했다.
- `LocalShellTask`, `LocalAgentTask`, `LocalMainSessionTask`, `RemoteAgentTask`, `InProcessTeammateTask`, `DreamTask`, `stopTask.ts`를 읽고 foreground/background 전환, stop transition, owner cleanup, sidecar restore, prompt stall watchdog, maintenance task 차이를 문서화했다.
- `coordinator/coordinatorMode.ts`를 읽고 coordinator mode를 worker capability projection, self-contained worker prompt, concurrency policy, verification discipline을 포함한 runtime mode로 정리했다.
- 새 분석 문서 `02-research/claude-code/task-coordinator-runtime.md`를 추가했다.
- Agentica Next 설계에는 internal `AgenticaTaskRuntime`, `AgenticaTaskOutputRef`, task notification public-surface 보류, coordinator runtime 초안을 반영했다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Claude Code Remote/Bridge/Server Runtime 보강

- Claude Code `remote/RemoteSessionManager.ts`, `remote/SessionsWebSocket.ts`, `remote/sdkMessageAdapter.ts`, `remote/remotePermissionBridge.ts`를 읽고 remote SDK message, permission/control request, reconnect, compact/status projection을 정리했다.
- `server/createDirectConnectSession.ts`, `server/directConnectManager.ts`, `server/types.ts`를 읽고 direct-connect session creation, stream-json WebSocket, permission/interrupt protocol을 문서화했다.
- `bridge/types.ts`, `bridge/bridgeApi.ts`, `bridge/bridgeMessaging.ts`, `bridge/replBridgeTransport.ts`, `bridge/replBridge.ts`, `bridge/bridgeMain.ts`, `bridge/sessionRunner.ts`, `bridge/createSession.ts`, `bridge/codeSessionApi.ts`, `bridge/workSecret.ts`를 읽고 environment work poll, ack/stop/archive/heartbeat, v1/v2 transport, sequence cursor, initial flush, multi-session capacity, worktree isolation, child process lifecycle을 정리했다.
- 새 분석 문서 `02-research/claude-code/remote-bridge-server-runtime.md`를 추가했다.
- Agentica Next 설계에는 internal `AgenticaRemoteSessionState`, `AgenticaRemoteMessageAdapter`, remote event public-surface 보류, permission interaction 분리, remote reconnect/heartbeat를 task failure와 분리하는 원칙을 반영했다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Claude Code State/Swarm Runtime 보강

- Claude Code `src/state/AppStateStore.ts`, `store.ts`, `AppState.tsx`, `selectors.ts`, `onChangeAppState.ts`, `teammateViewHelpers.ts`를 읽고 AppState를 UI state가 아니라 runtime state hub로 정리했다.
- `utils/swarm`의 team file registry, backend registry, pane executor, in-process backend, in-process runner, permission sync, teammate init/reconnection/spawn utilities를 읽었다.
- `utils/teammateContext.ts`, `utils/agentContext.ts`, `utils/teammateMailbox.ts`를 읽고 AsyncLocalStorage identity isolation, mailbox typed control message, idle/shutdown/permission/plan protocol을 정리했다.
- 새 분석 문서 `02-research/claude-code/state-swarm-runtime.md`를 추가했다.
- Agentica Next 설계에는 internal `AgenticaRuntimeStateStore`, `AgenticaRuntimeStateObserver`, `AgenticaTeamExecutor`, `AgenticaMailboxAdapter`, team/member runtime state, public-surface 보류 전략을 반영했다.
- `MicroAgentica`는 이번 보강 대상에서 계속 제외했다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Claude Code Plugin Marketplace/Install Runtime 보강

- Claude Code `utils/plugins/schemas.ts`, `pluginDirectories.ts`, `marketplaceManager.ts`, `reconciler.ts`, `installedPluginsManager.ts`, `pluginInstallationHelpers.ts`, `dependencyResolver.ts`, `pluginOptionsStorage.ts`, `pluginPolicy.ts`, `managedPlugins.ts`, `pluginBlocklist.ts`를 읽었다.
- `services/plugins/pluginOperations.ts`, `PluginInstallationManager.ts`, `utils/plugins/pluginAutoupdate.ts`를 읽고 install/update/autoupdate/uninstall/enable/disable operation boundary를 정리했다.
- 새 분석 문서 `02-research/claude-code/plugin-marketplace-runtime.md`를 추가했다.
- 핵심 결론은 plugin runtime을 settings intent, materialized cache state, runtime projection으로 분리하고, install은 settings-first, update는 non-inplace disk update 뒤 reload/restart boundary로 적용한다는 것이다.
- Agentica Next 설계에는 internal `AgenticaCapabilityRegistryState`, capability install/enablement state, dependency policy, secret userConfig 분리, public-surface 보류 전략을 반영했다.
- `MicroAgentica`는 이번 보강 대상에서 계속 제외했다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

## Claude Code Plugin UI/Validation/MCPB/Cache 보강

- Claude Code `commands/plugin/index.tsx`, `plugin.tsx`, `parseArgs.ts`, `PluginSettings.tsx`, `DiscoverPlugins.tsx`, `BrowseMarketplace.tsx`, `ManagePlugins.tsx`, `ManageMarketplaces.tsx`, `AddMarketplace.tsx`, `ValidatePlugin.tsx`, `PluginOptionsFlow.tsx`, `PluginOptionsDialog.tsx`를 읽었다.
- `services/plugins/pluginCliCommands.ts`, `utils/plugins/validatePlugin.ts`, `mcpbHandler.ts`, `mcpPluginIntegration.ts`, `zipCache.ts`, `zipCacheAdapters.ts`, `cacheUtils.ts`, `orphanedPluginFilter.ts`, `refresh.ts`를 읽고 `/plugin` control plane, validation, MCPB/DXT, zip cache, orphan search exclusion, reload projection boundary를 정리했다.
- 추가로 `commands/reload-plugins/*`, `main.tsx` startup orphan cleanup/exclusion cache 순서, `screens/REPL.tsx`, `utils/plugins/performStartupChecks.tsx`, `utils/plugins/headlessPluginInstall.ts`, `hooks/useManagePlugins.ts`, `cli/handlers/plugins.ts`, `cli/print.ts` SDK reload path를 읽고 reload/startup/headless/CLI validate 세부를 보강했다.

### MCP Auth/UI/Approval 보강

- Claude Code `services/mcp/auth.ts`, `xaa.ts`, `xaaIdpLogin.ts`, `oauthPort.ts`, `headersHelper.ts`, `officialRegistry.ts`, `commands/mcp/*`, `cli/handlers/mcp.tsx`, `components/mcp/*`, `services/mcpServerApproval.tsx`, `MCPServerApprovalDialog`, `MCPServerMultiselectDialog`를 추가 독해했다.
- `mcp-service-runtime.md`에 OAuth/PKCE, XAA, token refresh lock, step-up auth, needs-auth cache, auth pseudo-tool, `/mcp` UI/CLI, `.mcp.json` approval, headersHelper trust gate, official registry prefetch를 반영했다.
- Agentica 설계 문서에는 connector credential state, approval state, auth recovery public surface, verification item을 추가했다.
- 새 분석 문서 `02-research/claude-code/plugin-ui-validation-cache.md`를 추가했다.
- 핵심 결론은 capability registry에는 Discover/Installed/Marketplaces/Errors/Validate control plane, strict authoring validator, needs-config state, zip/session extraction backend, orphaned search exclusion, explicit reload transaction이 필요하다는 것이다.
- Agentica Next 설계에는 internal `AgenticaCapabilityControlPlaneState`, reload result, failed/flagged/needs-config recovery item, strict validation, connector reconnect/search exclusion reload 원칙을 반영했다.
- `MicroAgentica`는 이번 보강 대상에서 계속 제외했다.
- 이번 라운드도 `.wiki`만 개정했고 package source는 변경하지 않았다.

### Plugin MCPB Source Trust 추가 보강

- Claude Code `mcpbHandler.ts`, `mcpPluginIntegration.ts`, `schemas.ts`, `pluginLoader.ts`, `marketplaceHelpers.ts`, `dxt/zip.ts`를 추가로 읽었다.
- MCPB source 검사는 suffix와 schema URL/local path 조건이 중심이고, enterprise allow/block는 marketplace source에 적용된다는 점을 `plugin-ui-validation-cache.md`에 정리했다.
- plugin manifest 내부 MCPB URL은 별도 host allowlist 없이 marketplace provenance trust에 기대는 구조로 보이며, Agentica에는 artifact source allowlist를 별도 정책으로 두도록 설계 문서를 갱신했다.
- local `mcpServers` JSON/MCPB path는 `./` prefix만으로는 root containment가 충분하지 않으므로 Agentica validator에 `realpath` containment와 traversal fixture를 요구하도록 정리했다.
- Claude Code zip helper의 traversal/absolute path/zip bomb/file count/size 방어선과 MCPB manifest validation도 함께 문서화했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### MCP SDK/In-process Transport 추가 보강

- Claude Code `services/mcp/SdkControlTransport.ts`, `InProcessTransport.ts`, `vscodeSdkMcp.ts`, `entrypoints/sdk/controlSchemas.ts`, `entrypoints/sdk/coreSchemas.ts`, `cli/structuredIO.ts`, `cli/print.ts`, `services/mcp/client.ts`의 SDK MCP 연결부를 추가 독해했다.
- `mcp-service-runtime.md`에 SDK MCP control request bridge, `mcp_message`, `mcp_set_servers`, `mcp_status`, `mcp_reconnect`, `mcp_toggle`, `reload_plugins`, in-process linked transport, `claude mcp serve`, VSCode SDK MCP notification 경계를 보강했다.
- Agentica 설계 문서에는 `sdk-control`/`in-process`/`ide-adapter` transport ownership, dynamic connector set result, handshake pending, public history 비노출 원칙을 반영했다.
- 검증 전략에는 JSON-RPC id와 outer control request id correlation 분리, dynamic connector mutation serialization, SDK handshake deadlock 방지, in-process close cleanup, IDE notification 비노출 항목을 추가했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Swarm Tmux/iTerm2 Pane Backend 보강

- Claude Code `utils/swarm/backends/types.ts`, `TmuxBackend.ts`, `ITermBackend.ts`, `PaneBackendExecutor.ts`, `detection.ts`, `registry.ts`, `it2Setup.ts`, `It2SetupPrompt.tsx`, `teammateModeSnapshot.ts`를 추가 독해했다.
- `teamDiscovery.ts`, `components/teams/TeamsDialog.tsx`, `teamHelpers.ts`, `teammateLayoutManager.ts`를 확인해 pane id/backendType/hidden/idle/mode projection과 kill/shutdown/focus/mode control을 정리했다.
- `state-swarm-runtime.md`에 tmux native/external session command, iTerm2 `it2` command, pane hide/show, setup prompt 보안 cwd, spawn command/env/flag inheritance, mailbox initial prompt, cleanup boundary를 보강했다.
- Agentica 설계 문서에는 external-pane을 core가 아니라 optional adapter로만 두고, backend snapshot, spawn transaction, mailbox/control adapter, env allowlist, public surface 비노출, 검증 항목을 반영했다.
- `MicroAgentica`는 이번 보강 대상에서 계속 제외했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### CLI Transport/SDK Control/Bridge UI 보강

- Claude Code `cli/transports/SSETransport.ts`, `WebSocketTransport.ts`, `HybridTransport.ts`, `SerialBatchEventUploader.ts`, `WorkerStateUploader.ts`, `ccrClient.ts`, `transportUtils.ts`를 추가 독해했다.
- `entrypoints/sdk/controlSchemas.ts`, `coreSchemas.ts`, `cli/structuredIO.ts`, `cli/remoteIO.ts`, `cli/print.ts`의 SDK core/control schema와 stdin/stdout control routing을 확인했다.
- `bridge/bridgeUI.ts`, `bridge/bridgeStatusUtil.ts`, `bridgeMain.ts` status update/key handling/headless logger 주변을 읽고 QR/status renderer와 core remote runtime의 분리 원칙을 정리했다.
- `remote-bridge-server-runtime.md`에 SDK core/control taxonomy, WebSocket/Hybrid/SSE/CCR transport 차이, ordered event queue와 coalesced worker state, delivery ack, internal transcript event persistence, CLI renderer surface를 보강했다.
- Agentica 설계 문서에는 remote transport reliability class, worker epoch/delivery/sequence state, public surface 비노출 원칙, Remote/SDK transport 검증 항목을 반영했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Plugin Validate Fixture Audit

- Claude Code snapshot 전체에서 `test`, `fixture`, `__tests__`, `.test.`, `.spec.`, `mcpb`, `dxt`, `plugin.*validate`, `validate.*plugin`, `marketplace` 패턴을 검색했다.
- directory search에서는 `src/tools/testing` 외 별도 test/fixture 디렉터리가 확인되지 않았고, file search 결과도 plugin validate 관련 source file로만 수렴했다.
- `commands/plugin/ValidatePlugin.tsx`, `cli/handlers/plugins.ts`, `utils/plugins/validatePlugin.ts`를 다시 확인해 interactive `/plugin validate`와 CLI `claude plugin validate`의 coverage 차이를 정리했다.
- `plugin-ui-validation-cache.md`에 snapshot-wide fixture audit 결론을 추가하고, Agentica는 자체 validator fixture pack을 반드시 만들어야 한다고 명시했다.
- `verification-strategy.md`, `reading-ledger.md`, `completion-criteria.md`, `source-inventory.md`, `state-swarm-runtime.md`, `plugin-marketplace-runtime.md`의 잔여 항목을 갱신했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### PowerShell/REPL/Platform Tool 보강

- Claude Code `tools/PowerShellTool/*`, `utils/powershell/*`, `utils/shell/*`, `tools/REPLTool/*`, `tools.ts`, `processBashCommand.tsx`, `promptShellExecution.ts`를 읽었다.
- PowerShellTool이 BashTool 옵션이 아니라 Windows gate, PowerShell AST parser, cmdlet/alias canonicalization, provider/UNC/security checks, PowerShell-specific path/read-only validation을 가진 독립 policy engine임을 정리했다.
- `resolveDefaultShell`, hook shell selection, prompt frontmatter shell, input-box `!` routing이 서로 다른 routing concern임을 정리했다.
- REPL mode가 direct tool list에서 primitive tools를 숨기되, hidden primitive registry를 renderer/collapse/audit용으로 보존한다는 구조를 문서화했다.
- 새 분석 문서 `02-research/claude-code/powershell-repl-platform-tools.md`를 추가했다.
- `bash-tool-policy.md`, `operation-policy-state-design.md`, `verification-strategy.md`, `reading-ledger.md`, `completion-criteria.md`, `source-inventory.md`, `state-swarm-runtime.md`, `00-index.md`를 갱신했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Tool Permission Control Flow 보강

- Claude Code `hooks/useCanUseTool.tsx`, `hooks/toolPermission/PermissionContext.ts`, interactive/coordinator/swarm permission handler, `permissionLogging.ts`를 읽었다.
- `utils/permissions/permissions.ts`, `PermissionUpdate.ts`, `PermissionUpdateSchema.ts`, `permissionsLoader.ts`, `permissionSetup.ts`, `shadowedRuleDetection.ts`를 읽고 rule/mode/update/persistence/managed policy 구조를 정리했다.
- `services/tools/toolHooks.ts`를 읽고 PreToolUse hook allow가 deny/ask rule과 safety check를 우회하지 않는다는 불변식을 정리했다.
- `components/permissions/*`의 PermissionRequest router, file/shell/fallback prompt option을 읽고 UI component가 `PermissionUpdate[]`, feedback, updated input을 생성하는 adapter임을 정리했다.
- 새 분석 문서 `02-research/claude-code/tool-permission-control-flow.md`를 추가했다.
- `bash-tool-policy.md`, `tool-system.md`, `operation-policy-state-design.md`, `verification-strategy.md`, `reading-ledger.md`, `completion-criteria.md`, `source-inventory.md`, `00-index.md`를 갱신했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Slash Command Runtime 보강

- Claude Code `commands.ts`, `types/command.ts`, `processSlashCommand.tsx`, `main.tsx`, `screens/REPL.tsx`를 읽고 prompt/local/local-jsx command contract와 headless/bridge allowlist를 정리했다.
- `commands/compact/*`를 읽고 manual `/compact`가 일반 user message가 아니라 `LocalCommandResult`의 `compact` transaction으로 처리됨을 문서화했다.
- `commands/context/*`, `utils/analyzeContext.ts`를 읽고 `/context`가 raw transcript가 아니라 compact boundary, context collapse, microcompact가 반영된 API-view를 기준으로 분석함을 정리했다.
- `commands/memory/*`, `components/memory/MemoryFileSelector.tsx`, `utils/claudemd.ts`를 읽고 `/memory`가 persistent instruction file/control plane이며 headless command가 아님을 정리했다.
- 새 분석 문서 `02-research/claude-code/slash-command-runtime.md`를 추가했다.
- Agentica Next 설계에는 runtime command surface, manual compact command transaction, command surface 검증 항목을 반영했다.
- `MicroAgentica`는 이번 보강 대상에서 계속 제외했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Agentica Chat Renderer 보강

- Agentica `packages/chat/src/AgenticaChatApplication.tsx`, `movies/AgenticaChatMovie.tsx`, message renderer 전체, side panel renderer, `MarkdownViewer.tsx`를 읽었다.
- `@agentica/chat`가 runtime owner가 아니라 `Agentica | MicroAgentica` instance를 받는 React/MUI adapter이고, assistant/describe는 `join()` 후 최종 history만 append하는 final-only UI임을 정리했다.
- `systemMessage`가 큰 System card로 그대로 노출되고, `describe.executes`가 arguments/value 전체 JSON을 출력하므로 compact marker와 result budget은 UI display contract까지 고려해야 함을 문서화했다.
- `validate`/`jsonParseError`는 console-only, user multimodal content는 미렌더링, side price는 vendor/model 무관 고정 단가라는 현재 제약을 기록했다.
- BBS/Shopping/Uploader examples를 읽고 BBS는 `MicroAgentica` regression, Shopping/Uploader는 Agentica OpenAPI/HTTP arbitrary schema regression surface로 분류했다.
- 새 문서 `04-agentica/chat-renderer-current.md`를 추가하고, `rpc-chat-public-surface.md`, `impact-surfaces.md`, `package-inventory.md`, runtime public surface/verification 설계, reading ledger/completion criteria/index를 갱신했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Agentica Test CLI/Examples 보강

- Agentica `test/src/cli/index.ts`, `cli/micro.ts`를 읽고 interactive shopping Agentica CLI와 BBS MicroAgentica CLI를 자동 test runner와 분리해 정리했다.
- `test/src/examples/websearch.ts`, `websocket-client-main.ts`, `websocket-client-hook.ts`, `examples/internal/IWrtnWebSearchApplication.ts`를 읽고 MicroAgentica describe-disabled example과 RPC WebSocket partial/full listener example을 문서화했다.
- `test/src/internal/BbsArticleService.ts`, `IBbsArticle.ts`, `utils/ConsoleScanner.ts`를 읽고 BBS CRUD fixture, UUID/date-time/image URI/partial DTO/void return, readline helper의 회귀 의미를 정리했다.
- 새 문서 `04-agentica/test-cli-examples-current.md`를 추가했다.
- `benchmark-test-current.md`, `package-inventory.md`, `verification-strategy.md`, reading ledger/completion criteria/index를 갱신했고 `test` 영역은 1차 완료로 표시했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Slash Command Catalog 보강

- 사용자 지시에 따라 이번 범위도 `.wiki` 개정으로만 유지했다.
- Claude Code `src/commands.ts`, `types/command.ts`의 registry/filter/allowlist 구조를 다시 확인하고, 개별 slash command를 prompt workflow, session/transcript transaction, materialized view/export, workspace/permission/mode control, runtime/account state mutation, adapter/integration wizard로 catalog화했다.
- `clear`, `resume`, `branch`, `copy`, `export`, `model`, `fast`, `effort`, `permissions`, `hooks`, `plan`, `sandbox`, `login/logout`, `remote-control`, `install-github-app`, `commit`, `commit-push-pr`, `init`, `init-verifiers`, `review`, `security-review`, `pr-comments`, `statusline`, `insights`를 읽고 `02-research/claude-code/slash-command-catalog.md`를 추가했다.
- Agentica 설계에는 command result union, command-driven policy mutation, remote/headless allowlist, prompt workflow allowed operation scope, adapter intent 비노출 원칙을 반영했다.
- `MicroAgentica`는 이번 보강 대상에서 계속 제외했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Agentica CLI/Create Scaffold 보강

- Agentica `packages/cli/package.json`, `bin/index.mjs`, `src/index.ts`, `commands/start.ts`를 읽고 `agentica start --project` command, template choices, interactive prompt context, setup functions를 정리했다.
- `connectors.ts`, `packages.ts`, `fs.ts`, `utils.ts`를 읽고 connector list fetch, service/package naming, placeholder code insertion, package manager command, `.env` append behavior를 문서화했다.
- `start.test.ts`, `connectors.test.ts`, `fs.test.ts`, `packages.test.ts`, `index.test.ts`, `utils.test.ts`, vitest/msw/build config를 읽고 scaffold test coverage와 test gap을 정리했다.
- `packages/create-agentica/package.json`, `index.js`를 읽고 `create-agentica`가 `agentica` CLI `run()`을 호출하는 alias package임을 정리했다.
- 새 문서 `04-agentica/cli-scaffold-current.md`를 추가했다.
- `package-inventory.md`, `impact-surfaces.md`, `verification-strategy.md`, `source-inventory.md`, reading ledger/completion criteria/index를 갱신했고 `packages/cli`, `packages/create-agentica`는 1차 완료로 표시했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Agentica Website/Docs/Articles 보강

- `website/package.json`, `next.config.ts`, `app/layout.jsx`, `app/[[...mdxPath]]/page.jsx`, `mdx-components.jsx`, `_meta.ts` 계층을 읽고 Next/Nextra static docs 구조를 정리했다.
- `website/build/typedoc.js`, `playground.js`, `deploy.js`, `next-sitemap.config.js`를 읽고 TypeDoc API build, chat playground static copy, sitemap/pagefind, deploy script mismatch risk를 문서화했다.
- setup/core/event/history/micro/websocket/plugin docs를 읽고 stale `text` event/listener, `AgenticaHistory.Text`, `@agentica/pg-vector-selector`, CLI `--manager`/directory argument/template count 불일치를 정리했다.
- 루트 `articles/*.md`와 `website/public/articles/*.html`을 읽고 function-calling-first positioning 원고와 Dev.to redirect wrapper 표면을 분리했다.
- 새 문서 `04-agentica/website-docs-current.md`를 추가했다.
- `package-inventory.md`, `impact-surfaces.md`, `verification-strategy.md`, `source-inventory.md`, reading ledger/completion criteria/index를 갱신했고 `website`, `articles`, `docs`는 1차 완료로 표시했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Claude Code UI Adapter/Ink Runtime 보강

- Claude Code `src/ink/ink.tsx`, `renderer.ts`, `screen.ts`, `parse-keypress.ts`, event dispatcher, `AlternateScreen`, `ScrollBox`를 읽고 custom terminal renderer와 input/event/scroll/selection runtime을 정리했다.
- `Messages.tsx`, `Message.tsx`, `MessageRow.tsx`, `MessageResponse.tsx`, `OffscreenFreeze`, `Ratchet`를 읽고 message projection pipeline, static/dynamic row memo, offscreen freeze, long session cap/virtualization 원칙을 정리했다.
- `PromptInput.tsx`, `TextInput.tsx`, `screens/REPL.tsx`를 읽고 prompt input state machine, paste/image reference, external editor, history/search/typeahead, fullscreen transcript/search/export, `messagesRef` source-of-truth pattern을 정리했다.
- 새 분석 문서 `02-research/claude-code/ui-adapter-ink-runtime.md`를 추가했다.
- Agentica 설계에는 model context projection과 render projection 분리, adapter-local `AgenticaRenderMessageRow`, large result preview/reference, input/scroll/search/clipboard/editor state 비노출 원칙을 반영했다.
- `MicroAgentica`는 이번 보강 대상에서 계속 제외했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### BashTool Execution/Sandbox Runtime 보강

- 사용자 지시에 따라 이번 범위도 `.wiki` 개정으로만 유지했다.
- Claude Code `tools/BashTool/BashTool.tsx`, `utils/Shell.ts`, `utils/ShellCommand.ts`, `utils/shell/bashProvider.ts`, `tools/BashTool/shouldUseSandbox.ts`, `utils/sandbox/sandbox-adapter.ts`를 추가 독해했다.
- `utils/task/TaskOutput.ts`, `utils/task/diskOutput.ts`, `tasks/LocalShellTask/LocalShellTask.tsx`를 확인해 shell output, background task, progress polling, stall watchdog, notification queue를 정리했다.
- `utils/bash/ast.ts`, `tools/BashTool/bashPermissions.ts`의 tree-sitter security parse와 semantic check 일부를 확인해 parser unavailable/too-complex/fail-closed 원칙을 보강했다.
- `bash-tool-policy.md`에 execution pipeline, shell provider/spawn boundary, timeout/abort/background, TaskOutput/large output, LocalShellTask notification, sandbox adapter, AST command security gate를 추가했다.
- Agentica 설계에는 `AgenticaOperationRunState`, `AgenticaOutputReference`, `AgenticaSandboxPolicy`, sandbox/profile 분리, progress/background/notification transition, output projection 원칙을 반영했다.
- 검증 전략에는 shell execution state, large output reference, progress/background race, timeout/interrupt, stall watchdog, sandbox cleanup, preview/write exactness fixture를 추가했다.
- `reading-ledger.md`, `completion-criteria.md`, `source-inventory.md`를 갱신해 BashTool 실행/샌드박스 독해 갭을 줄이고 남은 범위를 `bashSecurity.ts` legacy edge-case와 feature-gated 세부로 좁혔다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### BashTool Legacy Security Gate 보강

- 사용자 지시에 따라 이번 범위도 `.wiki` 개정으로만 유지했다.
- Claude Code `tools/BashTool/bashSecurity.ts` 전체와 `utils/bash/shellQuote.ts`, `ParsedCommand.ts`, `treeSitterAnalysis.ts` 관련부를 추가 독해했다.
- `bashSecurity.ts`가 legacy regex blocklist가 아니라 shell-quote/bash parser differential을 잡는 parser trust gate라는 점을 정리했다.
- safe heredoc substitution, git commit early allow, jq 위험 flag, substitution/metacharacter, dangerous variable, IFS, `/proc/*/environ`, obfuscated flag, CR/Unicode/control whitespace, mid-word hash, comment quote desync, quoted newline, brace expansion, zsh dangerous command, malformed token validator 축을 문서화했다.
- `bash-tool-policy.md`에 Legacy bashSecurity validator 섹션을 추가하고, Agentica 설계에는 parser trust gate와 reason-class 분리 원칙을 반영했다.
- 검증 전략에는 parser divergence/unavailable, exact allow와 broad allow 차이, read-only auto-allow 선행 gate, early-allow fixture, obfuscation fixture 항목을 추가했다.
- `reading-ledger.md`, `completion-criteria.md`, `source-inventory.md`에서 `bashSecurity.ts` 잔여 갭을 제거하고 남은 BashTool 갭을 classifier/feature-gated 세부로 좁혔다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Query Loop Runtime 보강

- 사용자 지시에 따라 이번 범위도 `.wiki` 개정으로만 유지했다.
- Claude Code `src/query.ts`, `query/config.ts`, `query/deps.ts`, `query/tokenBudget.ts`, `query/stopHooks.ts` 전체를 다시 독해했다.
- `services/tools/StreamingToolExecutor.ts`, `services/tools/toolOrchestration.ts`를 확인해 streaming tool execution과 batch execution의 concurrency/order/context modifier 정책을 정리했다.
- `query-loop.md`에 context prep, tool result budget, snip/microcompact/context-collapse/autocompact 순서, streaming fallback/tombstone, withheld recoverable error, model fallback, tool execution, attachment drain, stop hooks, token-budget continuation을 보강했다.
- Agentica orchestration 설계에는 pending streaming transcript, operation execution policy, attachment drain phase, expanded transition reasons를 반영했다.
- 검증 전략에는 collapse drain, max-output recovery, model fallback tombstone, stop hook transition, attachment drain, memory/skill prefetch, operation concurrency/order/synthetic result fixture를 추가했다.
- `reading-ledger.md`에서 `src/query.ts`, `src/query/*`를 완료로 갱신했다. `query/transitions` type import 대상 source file은 snapshot에서 확인되지 않아 literal reason 기준으로 정리했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### ToolSearch/Local RAG Selector 보강

- 사용자 지시에 따라 이번 범위도 `.wiki` 개정으로만 유지했다.
- Claude Code `tools/ToolSearchTool/ToolSearchTool.ts`, `tools/ToolSearchTool/prompt.ts`, `utils/toolSearch.ts`를 정밀 독해했다.
- `services/api/claude.ts`, `utils/messages.ts`, `services/compact/compact.ts`, `utils/attachments.ts`, `services/mcp/client.ts`, `services/tools/toolExecution.ts`의 호출부를 따라가 `defer_loading`, `tool_reference`, discovered tool scan, compact boundary carry, delta attachment, pending MCP server, unavailable reference stripping을 확인했다.
- `context-rag-memory.md`에 ToolSearch를 DB 없는 lexical search가 아니라 deferred schema loading runtime으로 정리했다.
- `local-rag-selector.md`에 Agentica provider-neutral projection mode, selector mode, auto threshold, direct `select:`, required term, cache invalidation, discovered operation state, compact/resume 복원 설계를 보강했다.
- `verification-strategy.md`에는 local operation index의 direct select, required term, searchHint, auto threshold, provider fallback, pending connector, no schema leak, compact state restore, MicroAgentica 비변경 test를 추가했다.
- `reading-ledger.md`에서 `src/tools/ToolSearchTool`, `src/utils/toolSearch.ts`를 완료로 갱신했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Slash Command Heavy UI/Internal Audit 보강

- 사용자 지시에 따라 이번 범위도 `.wiki` 개정으로만 유지했다.
- Claude Code `commands.ts`를 다시 독해해 builtin, bundled skill, plugin, workflow, MCP command merge와 `REMOTE_SAFE_COMMANDS`/`BRIDGE_SAFE_COMMANDS`의 차이를 정리했다.
- `src/commands` 전체 파일 목록과 command index metadata를 대조해 기존 catalog에서 약했던 command를 확인했다.
- `btw`, `agents`, `skills`, `tasks`, `mcp`, `terminal-setup`, native `install`, `voice`, `chrome`, `desktop`, `remote-setup`, `keybindings`, settings-tab proxy, diagnostic/support command를 추가 독해했다.
- `components/agents/AgentsMenu.tsx`, `components/skills/SkillsMenu.tsx`, `components/tasks/BackgroundTasksDialog.tsx`를 확인해 agent/skill/task registry view/control 성격을 보강했다.
- `slash-command-catalog.md`에 side question, registry projection, local machine mutation, browser/device integration, connector direct mutation, feature-gated missing module audit를 추가했다.
- Agentica runtime public surface 설계에는 `sideQuery`, `registryMutation`, `localMachineMutation` command result class를 추가했다.
- 검증 전략에는 remote-safe/bridge-safe 분리, side query 비오염, local machine mutation audit, registry command projection, MCP mutation, voice/media permission, URL/token/path 비노출 test를 추가했다.
- `reading-ledger.md`에서 `src/commands/*`를 완료로 갱신했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### Ink/UI Renderer 2차 보강

- 사용자 지시에 따라 이번 범위도 `.wiki` 개정으로만 유지했다.
- Claude Code `src/ink/output.ts`, `render-node-to-output.ts`, `render-to-screen.ts`, `searchHighlight.ts`, `selection.ts`, `hooks/use-selection.ts`, `hooks/use-search-highlight.ts`, `hooks/use-terminal-viewport.ts`를 추가 독해했다.
- `Messages.tsx`, `MessageRow.tsx`, `OffscreenFreeze.tsx`의 가상 스크롤, render cap, UUID+index anchor, deterministic streaming row id, search text cache, static row memo comparator를 재확인했다.
- `ui-adapter-ink-runtime.md`에 Screen/Output packed buffer, grapheme/wide-char/OSC8 처리, scroll drain/DECSTBM hint, selection/search overlay, render projection 안정성 원칙을 보강했다.
- Agentica public surface 설계에는 adapter-local `AgenticaRenderProjectionState`, row static hint, row anchor, overlay state 비노출 원칙을 추가했다.
- 검증 전략에는 render cap anchor, grouping/collapse 안정성, deterministic streaming row id, full array prop pinning 방지, overlay 비오염, wide-char/soft-wrap/noSelect edge, terminal input 비노출 test를 추가했다.
- `reading-ledger.md`에서 Claude Code `src/components`, `src/ink`, `src/screens/REPL.tsx`를 완료로 갱신했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### 외부 Reference 1차 정리

- OpenAI Function Calling/Structured Outputs, Anthropic Tool Use/Prompt Caching/Context Editing/Compaction, MCP 2025-11-25 specification/tools/tasks, SQLite FTS5, React state/deferred/memo, Unicode UAX #29, JSON Schema 2020-12 문서를 확인했다.
- 새 문서 `02-research/external-references.md`를 추가해 Agentica Next의 tool loop, context compaction, connector public surface, no-DB selector, render projection 설계 판단과 공식 reference를 연결했다.
- `00-index.md`, `completion-criteria.md`, `reading-ledger.md`에 외부 reference 조사 상태를 반영했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.

### 남은 부분 영역 감사

- Claude Code `remoteBridgeCore.ts`, `jwtUtils.ts`, `pollConfig.ts`, `pollConfigDefaults.ts`를 읽고 env-less bridge token refresh, worker epoch rebuild, flush gate, poll config schema validation을 `remote-bridge-server-runtime.md`에 반영했다.
- `LocalShellTask.tsx`, `utils/task/framework.ts`, `utils/task/sdkProgress.ts`를 읽고 monitor kind, stall watchdog, task progress SDK projection, stale snapshot 방지 원칙을 `task-coordinator-runtime.md`에 반영했다.
- `toolExecution.ts`, `pathValidation.ts`의 classifier/wrapper stripping 세부를 읽고 `bash-tool-policy.md`에 speculative classifier, PermissionDenied retry, wrapper normalization sync를 반영했다.
- `mcpSkillBuilders.ts`, `useManageMCPConnections.ts`, memdir/session memory 관련부를 확인해 공개 snapshot에 없는 feature-gated seam과 MCP resources/prompts cache invalidation 차이를 `skill-plugin-loading.md`, `mcp-service-runtime.md`에 반영했다.
- `reading-ledger.md`의 남은 `부분` 영역을 실제 구현 포함 범위와 snapshot 미포함 feature-gated seam으로 감사해 `완료`로 정리했다.
- 이번 라운드도 `.wiki`만 개정했고 Agentica source package는 변경하지 않았다.
